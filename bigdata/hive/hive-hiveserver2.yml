---
apiVersion: v1
kind: Service
metadata:
  name: hiveserver2
  labels:
    app: hiveserver2
spec:
  ports:
  - port: 10000
    targetPort: 10000
    name: hiveserver2
  selector:
    app: hiveserver2
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: hiveserver2
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: hiveserver2
    spec:
      containers:
      - name: hiveserver2
        imagePullPolicy: IfNotPresent
        image: pilchard/hive
        resources:
          requests:
            cpu: "100m"
        livenessProbe:
          tcpSocket:
            port: 10000
          initialDelaySeconds: 15
          periodSeconds: 15
          timeoutSeconds: 5
        env:
        - name : HADOOP_HEAPSIZE
          value: "1000"   
        ports:
        - containerPort: 10000
          name: rpc
        command:
        - sh
        - -c
        - |
          hdfs dfs -ls /user/hive || su -s /bin/bash hdfs -c "hdfs dfs -mkdir -p /user/hive && hdfs dfs -chmod -R 1777 /user && hdfs dfs -chown -R hive:hive /user/hive"
          su -s /bin/bash hive -c "hive --service hiveserver2"
        volumeMounts:
        - name: hive
          mountPath: /etc/hive/conf   
        - name: hadoop
          mountPath: /etc/hadoop/conf                                   
      volumes:
      - name: hive
        configMap:
          name: hive
      - name: hadoop
        configMap:
          name: hadoop
          
