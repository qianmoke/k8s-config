apiVersion: v1
kind: Service
metadata:
  name: kibana
  namespace: kube-system
  labels:
    component: elasticsearch
    role: kibana 
spec:
  type: LoadBalancer
  selector:
    component: elasticsearch
    role: kibana
  ports:
  - name: http
    port: 5601
    protocol: TCP
